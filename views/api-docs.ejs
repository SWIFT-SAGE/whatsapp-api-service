<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>API Documentation - WhatsApp API Service</title>
    
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <!-- Prism.js for syntax highlighting -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/prism/1.29.0/themes/prism.min.css" rel="stylesheet">
    
    <style>
        :root {
            /* Use your professional color tokens */
            --color-cream-50: rgba(252, 252, 249, 1);
            --color-cream-100: rgba(255, 255, 253, 1);
            --color-slate-500: rgba(98, 108, 113, 1);
            --color-slate-900: rgba(19, 52, 59, 1);
            --color-teal-500: rgba(33, 128, 141, 1);
            --color-teal-600: rgba(29, 116, 128, 1);
            --color-brown-600: rgba(94, 82, 64, 1);
            --color-charcoal-700: rgba(31, 33, 33, 1);
            --color-charcoal-800: rgba(38, 40, 40, 1);
            
            /* Professional typography */
            --font-family-base: "FKGroteskNeue", "Geist", "Inter", -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
            --font-family-mono: "Berkeley Mono", ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace;
            
            /* Refined spacing and borders */
            --radius-sm: 6px;
            --radius-base: 8px;
            --radius-lg: 12px;
            --shadow-sm: 0 1px 3px rgba(0, 0, 0, 0.04), 0 1px 2px rgba(0, 0, 0, 0.02);
            --shadow-md: 0 4px 6px -1px rgba(0, 0, 0, 0.04), 0 2px 4px -1px rgba(0, 0, 0, 0.02);
        }

        body {
            font-family: var(--font-family-base);
            background-color: var(--color-cream-50);
            color: var(--color-slate-900);
            line-height: 1.6;
            font-size: 14px;
        }
        
        .navbar {
            background: var(--color-cream-100) !important;
            border-bottom: 1px solid rgba(94, 82, 64, 0.12);
            backdrop-filter: blur(12px);
        }
        
        .navbar-brand {
            font-weight: 600;
            color: var(--color-teal-500) !important;
            font-size: 18px;
            letter-spacing: -0.01em;
        }
        
        .nav-link {
            color: var(--color-slate-500) !important;
            font-weight: 500;
            font-size: 14px;
        }
        
        .nav-link:hover, .nav-link.active {
            color: var(--color-teal-500) !important;
        }
        
        .hero-section {
            background: linear-gradient(135deg, var(--color-slate-900) 0%, var(--color-charcoal-700) 100%);
            color: var(--color-cream-50);
            padding: 5rem 0;
            margin-bottom: 0;
            position: relative;
        }
        
        .hero-section::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: url('data:image/svg+xml,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100"><defs><pattern id="grid" width="10" height="10" patternUnits="userSpaceOnUse"><path d="M 10 0 L 0 0 0 10" fill="none" stroke="rgba(255,255,255,0.03)" stroke-width="0.5"/></pattern></defs><rect width="100" height="100" fill="url(%23grid)"/></svg>');
            opacity: 0.4;
        }
        
        .hero-section .container {
            position: relative;
            z-index: 1;
        }
        
        .hero-section h1 {
            font-size: 2.5rem;
            font-weight: 600;
            margin-bottom: 1rem;
            letter-spacing: -0.02em;
        }
        
        .hero-section p {
            font-size: 16px;
            opacity: 0.85;
            font-weight: 400;
        }
        
        .sidebar {
            background: var(--color-cream-100);
            border: 1px solid rgba(94, 82, 64, 0.12);
            border-radius: var(--radius-lg);
            box-shadow: var(--shadow-sm);
            padding: 1.5rem;
            position: sticky;
            top: 20px;
        }
        
        .sidebar h5 {
            color: var(--color-slate-900);
            font-weight: 600;
            font-size: 14px;
            text-transform: uppercase;
            letter-spacing: 0.05em;
            margin-bottom: 1rem;
        }
        
        .sidebar .nav-link {
            color: var(--color-slate-500) !important;
            padding: 0.75rem 0;
            border-bottom: 1px solid rgba(94, 82, 64, 0.08);
            text-decoration: none;
            font-size: 14px;
            font-weight: 500;
            transition: all 0.15s ease;
        }
        
        .sidebar .nav-link:hover {
            color: var(--color-teal-500) !important;
            padding-left: 0.5rem;
        }
        
        .sidebar .nav-link.active {
            color: var(--color-teal-500) !important;
            font-weight: 600;
            padding-left: 0.5rem;
            border-left: 2px solid var(--color-teal-500);
        }
        
        .section-title {
            color: var(--color-slate-900);
            font-weight: 600;
            font-size: 20px;
            margin-bottom: 1.5rem;
            padding-bottom: 0.75rem;
            border-bottom: 1px solid rgba(94, 82, 64, 0.12);
            letter-spacing: -0.01em;
        }
        
        .card {
            background: var(--color-cream-100);
            border: 1px solid rgba(94, 82, 64, 0.12);
            border-radius: var(--radius-lg);
            box-shadow: var(--shadow-sm);
            margin-bottom: 1.5rem;
        }
        
        .card-header {
            background: var(--color-slate-900);
            color: var(--color-cream-50);
            border-radius: var(--radius-lg) var(--radius-lg) 0 0 !important;
            border: none;
            padding: 1rem 1.5rem;
        }
        
        .card-header h5 {
            margin: 0;
            font-weight: 600;
            font-size: 16px;
        }
        
        .card-body {
            padding: 1.5rem;
        }
        
        .api-endpoint {
            background: rgba(33, 128, 141, 0.08);
            border: 1px solid rgba(33, 128, 141, 0.2);
            border-radius: var(--radius-base);
            padding: 0.5rem 0.75rem;
            display: inline-flex;
            align-items: center;
            gap: 0.5rem;
            font-family: var(--font-family-mono);
            font-size: 13px;
            margin-top: 0.5rem;
        }
        
        .badge {
            font-family: var(--font-family-mono);
            font-size: 11px;
            font-weight: 600;
            padding: 0.25rem 0.5rem;
            border-radius: var(--radius-sm);
        }
        
        .badge.bg-success {
            background-color: var(--color-teal-500) !important;
        }
        
        pre {
            background: var(--color-charcoal-800) !important;
            color: var(--color-cream-50);
            border: 1px solid rgba(94, 82, 64, 0.2);
            border-radius: var(--radius-base);
            padding: 1rem;
            font-family: var(--font-family-mono);
            font-size: 13px;
            line-height: 1.5;
            overflow-x: auto;
        }
        
        .code-block {
            background: var(--color-charcoal-800);
            color: var(--color-cream-50);
            padding: 1rem;
            border-radius: var(--radius-base);
            font-family: var(--font-family-mono);
            font-size: 13px;
            line-height: 1.5;
            overflow-x: auto;
        }
        
        .alert {
            border: 1px solid rgba(94, 82, 64, 0.2);
            border-radius: var(--radius-base);
            padding: 1rem;
            margin-bottom: 1.5rem;
        }
        
        .alert-info {
            background: rgba(33, 128, 141, 0.08);
            border-color: rgba(33, 128, 141, 0.2);
            color: var(--color-slate-900);
        }
        
        .alert-warning {
            background: rgba(168, 75, 47, 0.08);
            border-color: rgba(168, 75, 47, 0.2);
            color: var(--color-slate-900);
        }
        
        .btn {
            border-radius: var(--radius-base);
            font-weight: 500;
            font-size: 14px;
            padding: 0.5rem 1rem;
            transition: all 0.15s ease;
        }
        
        .btn-primary {
            background-color: var(--color-teal-500);
            border-color: var(--color-teal-500);
            color: var(--color-cream-50);
        }
        
        .btn-primary:hover {
            background-color: var(--color-teal-600);
            border-color: var(--color-teal-600);
        }
        
        .btn-outline-secondary {
            border-color: rgba(94, 82, 64, 0.3);
            color: var(--color-slate-500);
        }
        
        .btn-outline-secondary:hover {
            background-color: rgba(94, 82, 64, 0.08);
            border-color: rgba(94, 82, 64, 0.4);
        }
        
        .table {
            font-size: 14px;
        }
        
        .table th {
            background: rgba(94, 82, 64, 0.04);
            color: var(--color-slate-900);
            font-weight: 600;
            font-size: 13px;
            text-transform: uppercase;
            letter-spacing: 0.05em;
            border-color: rgba(94, 82, 64, 0.12);
        }
        
        .table td {
            border-color: rgba(94, 82, 64, 0.08);
            color: var(--color-slate-500);
        }
        
        .nav-pills .nav-link {
            border-radius: var(--radius-base);
            color: var(--color-slate-500);
            font-weight: 500;
            font-size: 14px;
        }
        
        .nav-pills .nav-link.active {
            background-color: var(--color-teal-500);
            color: var(--color-cream-50);
        }
        
        footer {
            background: var(--color-slate-900);
            color: var(--color-cream-50);
        }
        
        footer a {
            color: rgba(252, 252, 249, 0.7);
        }
        
        footer a:hover {
            color: var(--color-cream-50);
        }
        
        /* Professional spacing */
        .container {
            max-width: 1200px;
        }
        
        section {
            margin-bottom: 3rem;
        }
        
        /* Refined scrollbar */
        ::-webkit-scrollbar {
            width: 6px;
            height: 6px;
        }
        
        ::-webkit-scrollbar-track {
            background: rgba(94, 82, 64, 0.08);
        }
        
        ::-webkit-scrollbar-thumb {
            background: rgba(94, 82, 64, 0.3);
            border-radius: 3px;
        }
        
        ::-webkit-scrollbar-thumb:hover {
            background: rgba(94, 82, 64, 0.5);
        }
    </style>
</head>
<body>
    <!-- Navigation -->
    <nav class="navbar navbar-expand-lg navbar-light bg-white shadow-sm">
        <div class="container">
            <a class="navbar-brand" href="/">
                <i class="fas fa-code me-2"></i>WhatsApp API
            </a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav me-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="/">Home</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link active" href="/api-docs">API Docs</a>
                    </li>
                </ul>
                <ul class="navbar-nav">
                    <% if (user) { %>
                        <li class="nav-item">
                            <a class="nav-link" href="/dashboard">
                                <i class="fas fa-tachometer-alt me-1"></i>Dashboard
                            </a>
                        </li>
                        <li class="nav-item">
                            <span class="nav-link">
                                <i class="fas fa-user me-1"></i><%= user.name %>
                            </span>
                        </li>
                    <% } else { %>
                        <li class="nav-item">
                            <a class="nav-link" href="/login">Login</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/register">Sign Up</a>
                        </li>
                    <% } %>
                </ul>
            </div>
        </div>
    </nav>

    <!-- Hero Section -->
    <section class="hero-section">
        <div class="container">
            <div class="row align-items-center">
                <div class="col-lg-7">
                    <h1>Developer Documentation</h1>
                    <p class="lead">Enterprise-grade WhatsApp Business API integration for modern applications</p>
                    <div class="row mt-4">
                        <div class="col-md-4">
                            <div class="d-flex align-items-center mb-2">
                                <div class="me-3" style="width: 2px; height: 20px; background: rgba(252, 252, 249, 0.3);"></div>
                                <span style="font-size: 13px; font-weight: 500;">RESTful Architecture</span>
                            </div>
                        </div>
                        <div class="col-md-4">
                            <div class="d-flex align-items-center mb-2">
                                <div class="me-3" style="width: 2px; height: 20px; background: rgba(252, 252, 249, 0.3);"></div>
                                <span style="font-size: 13px; font-weight: 500;">Webhook Events</span>
                            </div>
                        </div>
                        <div class="col-md-4">
                            <div class="d-flex align-items-center mb-2">
                                <div class="me-3" style="width: 2px; height: 20px; background: rgba(252, 252, 249, 0.3);"></div>
                                <span style="font-size: 13px; font-weight: 500;">Rate Limiting</span>
                            </div>
                        </div>
                    </div>
                </div>
                <div class="col-lg-5">
                    <div class="text-end">
                        <div style="background: rgba(252, 252, 249, 0.08); border: 1px solid rgba(252, 252, 249, 0.12); border-radius: 8px; padding: 1.5rem;">
                            <div class="mb-2" style="font-size: 12px; font-weight: 600; text-transform: uppercase; letter-spacing: 0.05em; opacity: 0.7;">API Endpoint</div>
                            <code style="color: var(--color-cream-50); font-family: var(--font-family-mono); font-size: 14px; background: none;"><%= baseUrl || 'https://api.messingin.com/v1' %></code>
                            <button class="btn btn-sm mt-3" style="background: rgba(252, 252, 249, 0.1); border: 1px solid rgba(252, 252, 249, 0.2); color: var(--color-cream-50); font-size: 12px;" onclick="copyToClipboard('base-url-hero')">
                                Copy URL
                            </button>
                            <div id="base-url-hero" style="display: none;"><%= baseUrl || 'https://api.messingin.com/v1' %></div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Main Content -->
    <div class="container">
        <div class="row">
            <!-- Sidebar -->
            <div class="col-lg-3">
                <div class="sidebar">
                    <h5 class="mb-3"><i class="fas fa-book me-2"></i>Documentation</h5>
                    <nav class="nav flex-column">
                        <a class="nav-link active" href="#quick-start">Quick Start</a>
                        <a class="nav-link" href="#authentication">Authentication</a>
                        <a class="nav-link" href="#sessions">Sessions</a>
                        <a class="nav-link" href="#messages">Messages</a>
                        <a class="nav-link" href="#webhooks">Webhooks</a>
                        <a class="nav-link" href="#rate-limits">Rate Limits</a>
                        <a class="nav-link" href="#errors">Error Codes</a>
                        <a class="nav-link" href="#examples">Code Examples</a>
                    </nav>
                    
                    <% if (user) { %>
                    <div class="mt-4 p-3 bg-light rounded">
                        <h6><i class="fas fa-key me-2"></i>Your API Key</h6>
                        <code class="small"><%= user.apiKey ? user.apiKey.substring(0, 12) + '...' : 'Not generated' %></code>
                        <% if (!user.apiKey) { %>
                            <br><small class="text-muted">
                                <a href="/dashboard" class="text-decoration-none">Generate in dashboard</a>
                            </small>
                        <% } %>
                    </div>
                    <% } %>
                </div>
            </div>

            <!-- Content -->
            <div class="col-lg-9">
                <!-- Include the API docs content -->
                <%- include('pages/api-docs') %>
            </div>
        </div>
    </div>

    <!-- Footer -->
    <footer class="bg-dark text-light py-4 mt-5">
        <div class="container">
            <div class="row">
                <div class="col-md-6">
                    <h5>WhatsApp API Service</h5>
                    <p class="text-muted">Professional WhatsApp integration for businesses</p>
                </div>
                <div class="col-md-6 text-md-end">
                    <div class="d-flex justify-content-md-end gap-3">
                        <a href="/" class="text-light text-decoration-none">Home</a>
                        <a href="/api-docs" class="text-light text-decoration-none">API Docs</a>
                        <a href="/dashboard" class="text-light text-decoration-none">Dashboard</a>
                    </div>
                </div>
            </div>
        </div>
    </footer>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <!-- Prism.js for syntax highlighting -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/prism/1.29.0/components/prism-core.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/prism/1.29.0/plugins/autoloader/prism-autoloader.min.js"></script>

    <script>
        // Copy to clipboard function
        function copyToClipboard(elementId) {
            const element = document.getElementById(elementId);
            const text = element.textContent;
            navigator.clipboard.writeText(text).then(() => {
                // Show success feedback
                const btn = element.nextElementSibling;
                if (btn && btn.tagName === 'BUTTON') {
                    const originalText = btn.innerHTML;
                    btn.innerHTML = '<i class="fas fa-check"></i> Copied!';
                    btn.classList.add('btn-success');
                    setTimeout(() => {
                        btn.innerHTML = originalText;
                        btn.classList.remove('btn-success');
                    }, 2000);
                }
            }).catch(() => {
                alert('Failed to copy to clipboard');
            });
        }

        // Smooth scrolling for sidebar links
        document.querySelectorAll('.sidebar .nav-link').forEach(link => {
            link.addEventListener('click', function(e) {
                const href = this.getAttribute('href');
                if (href.startsWith('#')) {
                    e.preventDefault();
                    const target = document.querySelector(href);
                    if (target) {
                        target.scrollIntoView({ behavior: 'smooth' });
                        
                        // Update active link
                        document.querySelectorAll('.sidebar .nav-link').forEach(l => l.classList.remove('active'));
                        this.classList.add('active');
                    }
                }
            });
        });

        // Highlight current section on scroll
        window.addEventListener('scroll', () => {
            const sections = document.querySelectorAll('section[id]');
            const scrollPos = window.scrollY + 100;

            sections.forEach(section => {
                const sectionTop = section.offsetTop;
                const sectionHeight = section.offsetHeight;
                const sectionId = section.getAttribute('id');

                if (scrollPos >= sectionTop && scrollPos < sectionTop + sectionHeight) {
                    document.querySelectorAll('.sidebar .nav-link').forEach(link => {
                        link.classList.remove('active');
                        if (link.getAttribute('href') === `#${sectionId}`) {
                            link.classList.add('active');
                        }
                    });
                }
            });
        });
    </script>
</body>
</html>
