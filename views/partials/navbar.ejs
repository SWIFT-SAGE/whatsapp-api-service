    <!-- Navigation -->
    <nav class="navbar navbar-expand-lg navbar-dark fixed-top" id="main-navbar">
        <div class="container">
            <a class="navbar-brand " href="/">
                <i class="fab fa-whatsapp me-2"></i>
                    <strong>API MESSAGING</strong>
            </a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="/pricing">Pricing</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/api-docs">API Docs</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/contact">Contact</a>
                    </li>
                    <li class="nav-item" id="login-nav" <% if (typeof user !== 'undefined' && user) { %>style="display: none;"<% } else { %>style="display: block;"<% } %>>
                        <a class="nav-link" href="/login">Login</a>
                    </li>
                    <li class="nav-item" id="register-nav" <% if (typeof user !== 'undefined' && user) { %>style="display: none;"<% } else { %>style="display: block;"<% } %>>
                        <a class="nav-link" href="/register">Sign Up</a>
                    </li>
                    <li class="nav-item" id="get-started-nav" <% if (typeof user !== 'undefined' && user) { %>style="display: none;"<% } else { %>style="display: block;"<% } %>>
                        <a class="btn btn-primary ms-lg-3" href="/register">
                            <i class="fas fa-rocket me-2"></i>Get Started
                        </a>
                    </li>
                    <li class="nav-item dropdown" id="user-nav" <% if (typeof user === 'undefined' || !user) { %>style="display: none;"<% } else { %>style="display: block;"<% } %>>
                        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown">
                            <i class="fas fa-user-circle me-2"></i>
                            <span id="user-name"><% if (typeof user !== 'undefined' && user) { %><%= user.name || 'User' %><% } else { %>John Doe<% } %></span>
                        </a>
                        <ul class="dropdown-menu">
                            <li><a class="dropdown-item" href="/dashboard">
                                <i class="fas fa-tachometer-alt me-2"></i>Dashboard
                            </a></li>
                            <li><hr class="dropdown-divider"></li>
                            <li><a class="dropdown-item" href="#" id="logout-btn">
                                <i class="fas fa-sign-out-alt me-2"></i>Logout
                            </a></li>
                        </ul>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

